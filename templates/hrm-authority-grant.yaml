schema_version: agent_playbooks.hrm_authority_grant.v0.1
grant_id: "{{stable_grant_id}}"
session_id: "{{capsule_session_id}}"
hrm_id: "{{exact_hrm_id}}"
grant_type: "operator_function_acceptance|merge_release|live_effect_authority|hrm_closure"
issued_by: "{{authority_name_or_role}}"
issued_at: "{{iso8601_timestamp}}"
expires_at: null
repository: "{{canonical_repository_or_null}}"
candidate_sha: "{{exact_candidate_or_integrated_sha}}"
allowed_effects: []
conditions: []
one_time: true
